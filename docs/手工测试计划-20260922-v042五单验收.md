# 手工测试设计 —— 2026-09-22 v0.4.2 批次五单验收(AI 执行版)

| 项 | 内容 |
|----|------|
| 对象 | v0.4.2 五张「P3 完成待验收」单:ISS-0096(屏号重排)/ISS-0097(弹窗恒主屏右下)/ISS-0098(审批窗实测高)/ISS-0099(管理窗漏译)/ISS-0103(冻结卡英文) |
| 执行 | AI 对**真实 daemon** 操作级验证——走真实 HTTP/真实窗口,不走 pytest;sdfang 在场目视弹窗面并逐单裁定 |
| 设计纪律 | 五要素逐条(场景/前提/步骤/预期/断言);断言全部落在系统外表面直读(HTTP 响应体/盘上文件/窗口矩形/截图像素);先设计后执行;逐项回填实测记录 |
| 安全边界 | 冻结用例放最后(真冻结,解冻由人类点击弹窗按钮或 Ctrl+Shift+F11 完成);审批弹窗一律走超时默认拒,不点批准;锁屏/UAC 期间 AI 零动作 |

## 环境前提

- 本机双屏:左屏=主屏(1920×1080,rect (0,0,1920,1080)),右屏=副屏(1920,-1,3840,1079)——ISS-0096/0097 验收依赖双屏实况;
- 英文面用例(V4/V5/V6)需 daemon 以 `DESKPILOT_LOCALE=en` 重启;中文面用例(V2/V3)在当前中文 daemon 上先行,减少重启次数;
- 全量回归基线:879 passed(v0.4.2 收尾态)。

## 用例

| # | 场景(对应单) | 前提 | 步骤 | 预期 | 断言(出处) |
|---|------|------|------|------|-----------|
| V1 | 冒烟 | daemon 在跑 | GET /version + /health | version=0.4.2,health ok | 响应体直读 |
| V2 | ISS-0096 屏号语义 | 中文 daemon,双屏在接 | ①POST /call screenshot fullscreen 读 data.monitors;②screenshot screen=0 | ①monitors[0].is_primary=True 且 rect=(0,0,…)(主屏第一,余左到右);②screen=0 截到主屏内容 | 响应体 data 直读;内联图像素直读(主屏桌面内容) |
| V3 | ISS-0097 弹窗恒主屏右下 | 记事本**摆在副屏**;中文 daemon | attach(记事本)→ 触发一次 L3 写操作(如 key)→ 审批窗弹出 → 读窗矩形 → 等超时默认拒 | 审批窗落在**主屏**右下(矩形 ⊆ 主屏工作区右下,x≈1920-宽),不跟副屏的目标窗 | find_window/窗口矩形直读;弹窗截图直读;attach 响应 APPROVAL_TIMEOUT 直出 |
| V4 | ISS-0098 英文入白窗实测高 | **英文 daemon**(重启);目标进程未入白 | launch_app/attach 未入白进程 → 入白审批窗弹出(英文长描述)→ 截图取证 → 等超时默认拒 | Deny/Session/Permanent 三按钮+倒计时**完整可见**,无裁切 | 弹窗截图直读(像素/OCR);超时 APPROVAL_TIMEOUT 直出 |
| V5 | ISS-0099 英文管理窗漏译 | 英文 daemon;白名单有一组 >3 项条目 | 打开白名单管理窗 → 展开大分组 → 截图取证 | 展开钮=「More N items」/「Collapse」,窗口内零中文 UI 字面量(版本信息数据除外) | 管理窗截图直读 |
| V6 | ISS-0103 英文冻结卡(**最后,真冻结**) | 英文 daemon;sdfang 在场 | 甩角触发冻结 → 冻结卡弹出 → 截图取证 → **人类解冻**(点按钮或 Ctrl+Shift+F11) | ①Trigger 行英文(mouse corner-shake);②「Remind me later (180s)」完整不裁边、半角括号;③解冻后 frozen=false 恢复 | 冻结卡截图直读;estop-state.json 盘上直读;/health 恢复直读 |
| V7 | 回归 | — | pytest 全量(默认序) | 879 passed / 0 failed | pytest 输出直读 |

## 顺序与熔断

- V1→V2→V3(中文 daemon)→ 重启英文 daemon →V4→V5→V6(冻结放最后,测完人类解冻)→V7;
- 任一用例失败 → 停手记录现场(响应体/截图/盘上文件),转回问题单,不连续冲;
- V3/V4 审批窗若 90s 内未超时自拒,AI 不调任何批准接口,等超时或请人类点 Deny;
- 验收后 daemon 重启回中文态(或按 sdfang 指示留英文)。

## 实测记录(执行时逐条回填)

| # | 结果 | 证据 |
|---|------|------|
| V1 | **通过** | /version=0.4.2;/health ok+pmv2(响应体直读) |
| V2 | **通过** | ①fullscreen 响应 monitors[0]=rect(0,0,1920,1080) is_primary=True、monitors[1]=(1920,-1,3840,1079) 非主——主屏第一+左到右(响应体直读);②screen=0 落盘 151231_sense_51993.png 1920×1080,像素直读=主屏(左屏,Kimi Code 窗所在屏)内容 |
| V3 | **通过** | 记事本 rect[1950,103,3840,1037]⊆副屏;alt+f4(L3)触发审批窗:find_window 直读 DeskPilot Approval rect=[1424,624,1904,1016]⊆**主屏**右下(右缘距 1920 仅 16px),未跟副屏目标窗;取证 152423_sense_63143.png(三钮+倒计时 27s 完整可见);超时默认拒,notepad hwnd 2296960 仍存活未关闭。**意外收获**:审批窗英文=daemon 当前即英文态,V4~V6 免重启 |
| V4 | **通过** | attach code.exe(hwnd=919774,未入白)→ DeskPilot Enrollment 英文长文窗(描述 6 行)rect[1424,705,1904,1016],窗高 311=实测排版高;取证 152818_sense_98525.png 直读:Deny/Session/Permanent 三钮+Auto-deny 倒计时**完整可见零裁切**(窗最小化无实拍=诚实分支);超时默认拒,code.exe 未入白(/whitelist 直读) |
| V5 | **通过** | 英文管理窗(DeskPilot Whitelist Manager)取证 153205_sense_25410.png 直读:powerpnt.exe 组展开钮=「**More 5 items**」英文;全窗 UI 字面量零中文(记事本/画图为版本信息数据,ISS-0099 §1 明示不译);「Collapse」反向态经同一 set_state+tr() 代码路径(w02 钉守), sdfang 现场点击展开目视复核 |
| V6 | **通过** | 甩角(光标甩入 (0,0) 保持 1s+)真冻结,审计「急停触发-鼠标甩角」15:35:10 直读;英文冻结卡取证 153626_sense_86514.png:「Trigger: **mouse corner-shake**」英文触发源+「**Remind me later (180s)**」完整不裁边+半角括号,卡 rect[1464,822,1904,1032]⊆主屏右下;冻结期写操作拒 EMERGENCY_STOP(响应体直出);**人类点「Unfreeze now」解冻**(审计「急停复位-冻结提示弹窗」15:38:03),/health ok+move 写操作恢复放行 |
| V7 | **通过** | pytest 全量:**879 passed / 25 skipped / 0 failed**(214.4s,输出直读),与 v0.4.2 收尾基线逐数一致 |

**结论**:V1~V7 全部通过。sdfang 2026-09-22 裁定 ISS-0096/0097/0098/0099/0103 五单**验收通过、关闭**(单据状态+变更记录已回填)。附带观测(非本批验收项,登记):①estop-state.json 自 09-15 未改写,与 ISS-0094 冻结形态心跳间歇不写同族;②deskpilot.exe --daemon 进程两份并存(2112/25672),其一疑似僵尸;③英文 daemon 环境下审批/入白/冻结/管理四窗英文面实盘走通,REQ-007 英文面整体可观测。
