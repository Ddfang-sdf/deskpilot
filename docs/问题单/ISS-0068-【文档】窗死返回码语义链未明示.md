# ISS-0068:【文档】窗死返回码语义未文档化——NO_BINDING(绑定核销)而非 WINDOW_GONE

| 项 | 内容 |
|----|------|
| 问题单号 | ISS-0068 |
| 标题 | 绑定窗口关闭后的写操作返回 NO_BINDING(绑定随窗死核销),而非直觉的 WINDOW_GONE——机制如实但文档未明示,AI 收到 NO_BINDING 会去重 attach(正确),但若想找"窗口没了"语义会困惑 |
| 严重级 | 低(文档明示;行为本身正确) |
| 状态 | **完成**(2026-09-17;仅文档+钉测试,见变更记录 v0.2) |
| 提出 | 2026-09-08 手工测试 E-03 待查⑧;2026-09-10 转正建单 |

## 现象与证据

E-03 实测:窗死 → 写操作 → NO_BINDING(绑定已核销)。语义链:
窗死 → 绑定核销 → NO_BINDING(请先 attach);attach 时窗不在 →
WINDOW_GONE/未找到。两个码各有位置,但工具文档/错误码表未写这条链。

## 方向

错误码文档(README 或工具描述错误段)补语义链说明:
NO_BINDING=绑定无效(可能窗死/超时/从未绑定)→ 重新 attach;
attach 失败才知窗死活。约束:仅文档。

## 变更记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-09-10 | 转正建单(手工测试 E-03 待查⑧) |
| v0.2 | 2026-09-17 | **完成**。docs/功能设计说明书.md 原因码段补语义链明句:「窗死 → 绑定核销 → NO_BINDING」+重新 attach 自愈指引+attach 路径 TARGET_NOT_FOUND 分界;钉测试 p68(钉定语非散词——散词「重新 attach/窗口已关」在文档他处已存在,初版钉散词误绿,已修)。零代码(约束遵守) |
