# ISS-0076:【设计引入】REQ-002 详细设计整篇机制已被 ISS-0054 作废——照此文评审必打回

| 项 | 内容 |
|----|------|
| 问题单号 | ISS-0076 |
| 标题 | `REQ-002-桌面图标感知-详细设计.md` v0.1 的**核心机制描述(序对齐校验 + 按索引合并 + item0 同名口径)**已被 ISS-0054 的按名归并实装整体取代,但文档从未回填——全篇 13 处含作废机制,其中含 mermaid 判定树与 §11 错误语义表,照此文评审会得出与实际实现相反的结论 |
| 严重级 | **中**(文档是设计与验收的唯一依据;设计文档描述的机制已不存在,后续任何基于该文的设计变更、评审、回归定位都会被误导) |
| 状态 | **完成**(2026-09-17;方向①按现实现回填,13 处全改;残余「序对齐」均为作废标注) |
| 提出 | 2026-09-10(REQ-003 开工前自查;ISS-0054 于 2026-09-10 落地,v0.3.6 发布) |

## 1. 根因(实证链)

**机制层**:ISS-0054 的修复动作是**替换装配机制本身**(索引对齐 → 按名归并),而非修 bug。任何"改机制"的修复都必然使其设计文档的机制章节整体失效——这是"改行为必改文档"纪律的高危面:**行为改了,文档没跟上,且没有闸门能自动发现**。

ISS-0054 的变更记录只写了"待评审 → 已修复",**未列出受影响的下游文档清单**,故回填遗漏。

## 2. 逐处清单(实证:文件:行)

| # | 行 | 原文(作废内容) | 现实现 |
|---|----|----------------|--------|
| 1 | `:24` | `序对齐假设 A-2(实现期校验)` | 假设已作废(按名归并,无序号依赖) |
| 2 | `:31` | `序对齐 \| UIA 枚举序 == ListView 索引序 == IFolderView 枚举序(A-2)` | 该等价关系**已证明不成立**(ISS-0054 序分叉实证) |
| 3 | `:42` | `DesktopIconAssembler \| 序对齐校验、按索引合并…` | 实为按名归并 + 双 fail-closed 分支 |
| 4 | `:80` | `assemble(region=None) → [IconEntry] \| 序对齐校验→按索引合并三路…` | 按名归并三路 |
| 5 | `:91` | `序对齐证据 \| 三路 \| item0 名称一致 \| 实现期校验留痕` | **`desktop_icons.py` 全文零 `item0`** |
| 6 | `:117` | mermaid:`序对齐校验(三路 count 一致 ∧ item0 同名;不一致→停手报错)` | 无此节点 |
| 7 | `:118` | mermaid:`按索引合并 IconEntry×N` | 按名归并 |
| 8 | `:141` | mermaid 时序:`序对齐校验→按索引合并→region 过滤` | 按名归并 |
| 9 | `:156` | mermaid `源路径 --> 合并过滤: 序对齐通过` | 无此判定 |
| 10 | `:216-224` | **§9.4 序对齐校验(mermaid 判定树)**:`B -- 否 --> C[报错:序对齐失败]` / `D{item0 三路同名?}` / `D -- 否 --> …` | 该分支树整体不存在;实为"归并失败:图标重名歧义"与"归并失败:图标 X 无匹配来源" |
| 11 | `:264` | `序对齐失败(count 不一致/首项不同名) \| INTERNAL_ERROR \| 含三路 count 直出` | 错误语义已变;count 不一致仍 fail-closed,但首项同名检查已删 |
| 12 | `:283` | 单元测试口径:`断言合并/过滤/序对齐/报错归约` | 测试设计侧见 ISS-0078 |
| 13 | `:286` | `序对齐校验函数 \| count 不一致/首项不同名两分支(直出)` | 该函数不存在 |

**已验证不在文档中残留的项**:`grep -n "item0" deskpilot/executor/desktop_icons.py` → 零命中;`grep -n "ClientToScreen|ScreenToClient"` → 零命中(坐标问题另立 ISS-0075)。

## 3. 整改方向(待评审)

| # | 方向 | 说明 |
|---|------|------|
| ①(倾向) | **按现实现回填详细设计**:删除 A-2 序对齐假设与 §9.4 判定树,改写为"按名归并 + 两条 fail-closed 分支(重名歧义/实体无源)",错误语义表同步改为实装错误码 | 文档回到"描述现状"的基准 |
| ② | 保留作废机制并标注"已作废(ISS-0054)" | 保留历史可追溯,但设计文档读者会被两套机制混淆 |
| ③ | 在文档头加"本版已被 ISS-0054 取代,详见 …"横幅,正文不动 | **不倾向**:正文 13 处仍会被引用 |

## 4. 约束

- 回填属**文档改动**,不涉及代码;但按"文档随代码走"须与实装逐字核对,禁止凭记忆改写;
- 回填时须同时给出**该文档受影响章节的完整清单**(本次自查已产出 13 处,回填以该表为准);
- 回填后须由 sdfang 复核一次,确认与 `desktop_icons.py` 一致。

## 5. 变更记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-09-10 | 建单(13 处逐点实证),待评审 || v0.2 | 2026-09-17 | **完成**(方向①,自主推进授权)。13 处逐点回填(§2 清单为准):A-2 假设/术语表/装配职责/assemble 行/证据行/三处 mermaid/§9.4 判定树重写为「计数一致性校验→按名归并(重名歧义/无匹配来源两分支 fail-closed)」/§11 错误语义表拆两行(计数不一致+归并失败)/§13 测试口径两处。逐字核对自 desktop_icons.py 实装(assemble :413-445 计数校验保留+按名归并+双分支+_norm_icon_name 规范化键+虚拟项 null);残余「序对齐/item0」仅存在于作废标注句。待 sdfang 复核(§4 约束项,随验收) |
