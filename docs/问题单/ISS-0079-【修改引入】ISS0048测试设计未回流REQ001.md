# ISS-0079:【修改引入】ISS-0048 承诺的测试设计未回流 REQ-001——回归用例 `test_mouse16b` 在设计中零处出现

| 项 | 内容 |
|----|------|
| 问题单号 | ISS-0079 |
| 标题 | ISS-0048(修改引入,高)承诺"测试设计(五要素):核心用例——替身 mouseUp 抛 FailSafeException,构造 Executor 不崩+审计拦截事件(直出);正常路径回归",且其 §3 明写 P1 红由 `test_mouse16b` 承担——但 `REQ-001-鼠标能力补全-测试设计.md` 中 **`test_mouse16b` / `FAILSAFE` / `ISS-0048` 零命中**,该回归用例**未被纳入测试设计**;状态栏仍写"**25 用例全绿**"而文件实为 **26** 条 |
| 严重级 | **中**(修改引入的回归用例不进设计文档 = 该用例**没有设计出处**;后续任何重构可能删掉它而无人知晓,同类崩溃(启动即崩)会静默复发。且测试设计的状态栏与实装计数不符,验收依据失真) |
| 状态 | **完成**(2026-09-17;TC-MOUSE-16b 回流 §0 映射+计数 25→26 修正) |
| 提出 | 2026-09-10(REQ-003 开工前自查) |

## 1. 根因(实证链)

**机制层**:ISS-0048 的整改方案在**问题单内**承诺了测试设计,但**未落到测试设计文档**(`docs/需求/REQ-001-鼠标能力补全/REQ-001-鼠标能力补全-测试设计.md`)。问题单是**临时记录**,测试设计文档是**长期验收依据**——承诺写在问题单里而不落文档,等于该用例进了代码、没进设计。

与 ISS-0076/0077/0078 同源:**修复动作落地了,下游文档未回流**。本单是 REQ-001 侧的同类实例。

## 2. 逐点实证

### 2.1 ISS-0048 的承诺原文

`docs/问题单/ISS-0048-...md:30-31`

```
- 测试设计(五要素):核心用例——替身 mouseUp 抛 FailSafeException,
  构造 Executor 不崩+审计拦截事件(直出);正常路径回归(三键抬起+审计)。
```

同单 `:35` 进一步指明实现载体:`P1 红:test_mouse16b 替身 mouseUp 抛 FailSafeException → 构造穿透(确认)`。

### 2.2 测试设计文档零命中

`grep -n "16b\|0048\|FAILSAFE" docs/需求/REQ-001-鼠标能力补全/REQ-001-鼠标能力补全-测试设计.md` → **零命中**。

### 2.3 状态栏计数与实装不符

| 位置 | 内容 |
|------|------|
| 设计文档 `:8` | `\| 状态 \| 已实施(tests/test_mouse_req01.py,**25 用例全绿**) \|` |
| 实测 | `grep -n "def test_" tests/test_mouse_req01.py` → **26** 条 |

差 **1** 条,正是 ISS-0048 引入的 `test_mouse16b_sweep_tolerates_corner_failsafe`(实装位置 `tests/test_mouse_req01.py:385`)。

> **诚实标注**:25→26 的差值是**本单实测的推论**(25 + ISS-0048 新增 1 = 26),未逐条比对用例清单。回填前须**逐行核对**,禁止据本单数字直接改写文档。

### 2.4 同族先例

同文件家族的计数历史:`190ac1e` 时 25 用例,`test_mouse_req01.py` 现 26 条——REQ-001 交付时 25 条的计数**当时正确**,是 ISS-0048 之后未更新。**这不是历史错误,是本次修改引入的新缺口。**

## 3. 整改方向(待评审)

| # | 方向 | 说明 |
|---|------|------|
| ①(倾向) | **把 `test_mouse16b` 作为正式用例纳入测试设计**:补一行五要素(场景:启动清扫遇光标压角;前提:替身 `mouseUp` 抛 `FailSafeException`;步骤:构造 Executor;预期:构造不崩 + 审计记「清扫被 FAILSAFE 拦截」;断言:替身异常注入后 `Executor()` 正常返回 + 审计事件名直出);状态栏计数按实测改正;§0 映射补 MOUSE-15/16 相应行 | 回归用例获得设计出处,后续重构有据可依 |
| ② | 只改状态栏计数 | 治标:用例仍无设计出处 |
| ③ | 在测试设计里加一节"问题单引入的用例"集中登记 | 可扩展性好,但会形成"设计与问题单双轨",**不倾向**——测试设计应是唯一验收依据 |

## 4. 约束

- **本单 §2.3 的差值为实测推论**,回填前必须逐条比对用例清单;
- 回填属**文档改动**;须与 `tests/test_mouse_req01.py` 逐条核对,禁止凭记忆;
- 该用例的**断言必须能被 ISS-0048 的机制变更区分**(即:若去掉容错,该用例必红)——回填时须确认这点,否则重蹈 ISS-0078 §2.2 的"免疫用例"覆辙;
- 与 ISS-0076~0078 **同源不同批**:那三张是 REQ-002 侧(ISS-0054 引发),本单是 REQ-001 侧(ISS-0048 引发)。

## 5. 变更记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-09-10 | 建单(承诺原文 + 文档零命中 + 计数实测),待评审 |
## 变更记录(补)

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.2 | 2026-09-17 | **完成**(自主推进授权)。REQ-001 测试设计:§0 映射 MOUSE-04 行补 TC-MOUSE-16b(test_mouse16b_sweep_tolerates_corner_failsafe,ISS-0048 承诺的回归用例);状态栏计数 25→26(实装 `grep -c "def test_"` 直数=26);演进记录 v0.3 入库。孤儿清零 |
