# 晨报：ISS-0016~0019 全部实现完毕（2026-09-02）

> 给你审核的工作汇总。四单全部实现+测试+本地提交，**未推送**（按你的指令）。

## 一、验收结论速览

| 单 | 主题 | 状态 | 用例 | 全量 |
|----|------|------|------|------|
| ISS-0016 | 窗口定位与读取可靠性（UIA COM 初始化/错误分类/搜索指引） | ✅ 已关闭 | 7 条先红后绿 | 410 |
| ISS-0018 | 路径与配置一致性（截图绝对路径/policy 单源化） | ✅ 已关闭 | 3 条先红后绿 | 413 |
| ISS-0017 | 窗口激活容错与提权边界（激活退避/ELEVATION_REQUIRED/遮挡判定） | ✅ 已关闭 | 8 条先红后绿 | 421 |
| ISS-0019 | 审批批量授权模型 | ⚠️ 实现完成，**1 项待你裁决**（见 §四） | 6 条先红后绿 | 427 |

最终全量：**427 passed, 1 skipped（环境守卫）**。daemon 已用全部修复后的最新构建重启（health ok)。

## 二、各单要点（审核入口：docs/问题单/ISS-0016~0019）

**ISS-0016**：executor 线程级 `CoInitialize` 惰性幂等（`_element_root` 单入口），COM 通道异常与窗口消失分流（`INTERNAL_ERROR「UIA 通道异常」` vs `WINDOW_GONE`)，find_window 零命中给"改按进程名/句柄"指引，ocr 补 OCR/视觉分工。**新增集成守门**：真实 HttpDaemon+真实 executor 实调 get_ui_tree 读真实记事本非空——消灭"executor 全替身"装配盲区。

**ISS-0018**:screenshot 返回绝对路径（`_save_shot` 目录 resolve);`dist\policy.yml` 已与 repo 真源同步（**哈希一致，approval_ttl=90 终于生产生效**);INSTALL.md 注明 policy.yml 是构建同步产物。

**ISS-0017**:activate 容错（SW_RESTORE+AttachThreadInput+前台锁 3 次短退避，失败诚实 False)；写前提权边界 fail-closed——目标以管理员运行而自身非管理员 → 新错误码 `ELEVATION_REQUIRED`（含"请以管理员身份重启 daemon"指引），检测失败同样拒绝；遮挡判定 `WINDOW_OCCLUDED` **置于激活之后**（激活失败仍 WINDOW_GONE 防误射，兼容旧语义）。提权 daemon 临时方案已被正式检测机制取代。

**ISS-0019**：令牌 `scope=once/window_session`（仅内存）、`session_scope_of` 接缝、闸四批量命中不弹窗（执行前复核照常）、常规审批弹窗新增「此后同类允许」按钮、终端类与 key 类按批准约束永远逐操作。

## 三、本地提交（未推送，共 5 个）

```
87c8220 feat: ISS-0019 审批批量授权模型
919eb5f fix: ISS-0017 窗口激活容错与提权边界
8dedb66 fix: ISS-0018 路径与配置一致性
ec8bba2 fix: ISS-0016 窗口定位与读取可靠性
（另含 ISS-0015 划界版描述+TC-BOUND 测试,随 ec8bba2 一并入库)
```

## 四、⚠️ 头号待裁决：ISS-0019 §6 约束自洽性缺陷

实现期实证：**当前闸四（eff==L3）的全部触发来源 = ①终端类 attach ②L3 危险键，而两者都在你批准的豁免面内**——批量授权当前**无任何生效面**（能触发闸四的都被豁免，不被豁免的到不了闸四）。

选项（详见 ISS-0019 §6):
1. **（推荐）终端类纳入批量**（批量正是为终端风暴而生），仅危险键保持逐次；
2. 维持现约束（批量作未来 L3-cap 条目的预备能力，当前空置）;
3. 改白名单 max_level=L3 语义为"该进程一切操作升 L3"（影响面大，不推荐）。

**我没有私自放宽约束**——按批准文本实现并把缺陷如实挂上，等你拍板。

## 五、运行环境

- **电源**：睡眠/关屏均已禁用（整夜跑通）。**你回来后还原**:
  ```powershell
  powercfg /change standby-timeout-ac 30
  powercfg /change monitor-timeout-ac 10
  ```
- daemon：最新构建、非提权运行中；需要操作提权应用时 `Start-Process dist\deskpilot.exe -ArgumentList "--daemon" -Verb RunAs`(UAC 人类确认，ISS-0017 D 已写入 README/INSTALL 待核)。
- 右半屏的 fresh Claude 会话仍在（昨夜的划界验收现场）。

## 六、遗留（非本夜范围）

- ISS-0012 仍"开发完成待实盘验收"（双屏 §4.2 等你环境）;
- ISS-0015 划界版已随 ec8bba2 入库，实盘验收你已做过（两测试线通过），可关单；
- 发版窗口：ISS-0012~0019 共 8 单的产出已远超 v0.3.1，建议审核后走 v0.3.2 流水线（逐步问你确认）。
