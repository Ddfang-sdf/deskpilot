# ISS-0036：【设计遗漏】鼠标操作能力面补全

| 项 | 内容 |
|----|------|
| 问题单号 | ISS-0036 |
| 标题 | 设计承诺"AI 像人一样操控鼠标键盘",实现仅覆盖左键单击/左键拖拽/垂直滚轮——右键/中键/双击/多键拖拽/按住不放/水平滚轮全部缺失 |
| 严重级 | **高**（sdfang 裁定:重大事故——设计遗漏,非实现小缺口;演示准备干跑实证:整理桌面图标因无"空白坐标右键"无法执行） |
| 状态 | **建单,待评审**(sdfang 追加裁定:能力必须可组合——按住+拖动、左右键同按等,给 AI 的工具要有人持鼠标的全自由度) |
| 提出 | 2026-09-05(演示视频干跑准备,sdfang 拍板全量补齐) |

## 1. 认账

详细设计说明书写明"AI 可以像人一样操控鼠标键盘"——该承诺自 M1 落地
起即未兑现:四道闸/审批/审计对鼠标操作的治理完备,但**能力面本身残缺**。
根因:实现期按"最小可用"裁剪,承诺与交付之间的差距从未被盘点;此次
演示准备成为第一枚试金石。属设计遗漏(非修改引入),按同等严重级认账。

## 2. 整改方案(三层能力面,组合自由+安全网)

### 2.1 原语层(组合基座,新增)

| 工具 | 语义 |
|------|------|
| `mouse_down` | `{token, button(left/right/middle)}` 按下指定键 |
| `mouse_up` | `{token, button(left/right/middle)}` 抬起指定键 |

左右键同按=`mouse_down(left)`+`mouse_down(right)`(状态共存,人类同款);
按住拖动=`mouse_down`+`move`+`mouse_up`;任何人类摆弄鼠标的序列都可拼出。

**安全网(原语暴露的前提,三者并立)**:
① 急停冻结 → 强制抬起全部按下键;
② 看门狗:任一键按下超 30s → 自动抬起+审计「悬空按键自愈」;
③ `mouse_up` 幂等(无对应按下时 no-op,防 AI 重试误抬)。

### 2.2 动词层(常用组合便捷化)

| 项 | 能力 | 设计 |
|----|------|------|
| A | 右键/中键单击 | `click` 增 `button`(默认 left) |
| B | 双击/三连击 | `click` 增 `clicks`(1/2/3,默认 1) |
| C | 多键拖拽 | `drag` 增 `button`(默认 left) |
| D | 按住不放 | `hold`:`{button, duration_ms}`——按下→等时长→抬起,finally 保证抬起 |
| E | 水平滚轮 | `scroll` direction 增 left/right |

### 2.3 文字层(对称)

`click_text` 增 `clicks`,button 扩展 middle。

### 约束

- 原语工具 L2;button 非法值 → INVALID_PARAMS(fail-closed);
- 按下状态由执行层跟踪,安全网三条款为硬约束(任一缺失评审不过);
- 证据图/遮挡校验:verb 层照常;原语层 down/up 只记审计不拍图(高频组合
  下的证据成本控制——组合动作的取证由 AI 自行 screenshot)。

## 3. 测试设计(五要素)

层级:单元(FakeExecutor/pyautogui 替身,断言=指令参数/替身计数直出)
+ 既有集成回归。

| 用例 | 场景 | 前提 | 步骤 | 预期结果 | 断言代码 |
|------|------|------|------|----------|----------|
| TC-MOUSE-01 | 右键单击(A) | 绑定窗 | click(x,y,button=right) | pyautogui.click 收到 right | 替身计数直出 |
| TC-MOUSE-02 | 中键单击(A) | 同上 | button=middle | 收到 middle | 直出 |
| TC-MOUSE-03 | 双击(B) | 同上 | clicks=2 | 收到 clicks=2 | 直出 |
| TC-MOUSE-04 | 非法按钮(A,单元) | 同上 | button=shift | INVALID_PARAMS,零派发 | reason_code+instructions 空(直出) |
| TC-MOUSE-05 | 右键拖拽(C) | 绑定窗 | drag(button=right) | mouseDown/mouseUp 收到 right | 替身直出 |
| TC-MOUSE-06 | 按住不放(D) | 绑定窗 | hold(duration_ms=500) | down→sleep→up 顺序,恰一次 | 替身调用序列直出 |
| TC-MOUSE-07 | 按住异常必抬起(D) | sleep 抛异常 | hold | 异常透出且 mouseUp 仍被调(finally) | 异常+计数直出 |
| TC-MOUSE-08 | 水平滚轮(E) | 绑定窗 | scroll(direction=left) | hscroll 收到 -amount | 替身直出 |
| TC-MOUSE-09 | click_text 双击(F) | OCR 命中 | click_text(clicks=2) | 收到 clicks=2 | 直出 |
| TC-MOUSE-10 | 模式注册 | TOOL_SCHEMAS | 检查全部鼠标工具 | button/clicks/hold/原语参数与默认值齐 | 形态断言(直出) |
| TC-MOUSE-11 | 左右同按(原语) | 绑定窗 | down(left)→down(right)→up(right)→up(left) | 两键状态共存,逐键抬起 | 替身调用序列直出 |
| TC-MOUSE-12 | 按住+拖动组合(原语) | 绑定窗 | down(left)→move→up(left) | 序列完整 | 直出 |
| TC-MOUSE-13 | 急停强制抬键(安全网①) | 按下后触发急停 | estop 冻结 | 全部按下键被 up | 替身直出 |
| TC-MOUSE-14 | 看门狗自愈(安全网②) | 按下后时钟推进 30s | 看门狗 tick | 自动 up+审计「悬空按键自愈」 | 替身+审计 JSONL 直出 |
| TC-MOUSE-15 | up 幂等(安全网③) | 无按下状态 | mouse_up(left) | no-op 不报错不误抬 | 直出 |

### 交叉面清单(§2.1)

| 触及对象 | 其他写入者/读取者 | 覆盖 |
|---------|-----------------|------|
| 按下状态(执行层新状态) | 写入:mouse_down/mouse_up/hold/drag;读取:estop 冻结、看门狗、审计 | TC-MOUSE-11~15 |
| executor._click/_drag/_scroll | 消费方:dispatch/click_text 链路;test_elements 打桩面 | TC-MOUSE-01~09 |
| TOOL_SCHEMAS | 消费方:validate_call/_list 描述面;test_validation 计数(24→新增数) | TC-MOUSE-10+既有 |
| pyautogui | 多键按下共存语义:down(left)+down(right) 状态独立(实现期实测留痕) | TC-MOUSE-11 |
| 审批面 | 原语/verb 全 L2 不新增审批路径(豁免声明) | 无新增 |

## 4. 变更记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-09-05 | 建单(演示干跑实证+能力面全量盘点;sdfang 裁定重大事故/设计遗漏,认账入 §1),待评审 |
| v0.2 | 2026-09-05 | sdfang 追加裁定:能力必须可组合(按住+拖动/左右同按)——推翻"禁裸原语"约束,重构为三层(原语层+动词层+文字层),原语配三重安全网(急停抬键/看门狗自愈/up 幂等),用例 10→15 |
