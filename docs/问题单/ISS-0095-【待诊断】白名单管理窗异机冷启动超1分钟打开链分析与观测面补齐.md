# ISS-0095:【待诊断】白名单管理窗异机冷启动超 1 分钟——打开链分析与观测面补齐

| 项 | 内容 |
|----|------|
| 问题单号 | ISS-0095 |
| 标题 | 白名单管理窗在异机(0.4.1 冻结形态)打开超过 1 分钟,本机秒开;打开链上无任何计时日志,无法定位慢在哪段——先补观测面,再按证据定整改 |
| 严重级 | **中**(管理面可用性;异机实测超 1 分钟 vs 本机秒开) |
| 状态 | **建单待诊断**(观测面方案已含;待异机实测取证) |
| 提出 | 2026-09-18 sdfang 报障:「另一台电脑上弹出的非常慢,基本上超过一分钟;这台电脑上弹出很快」 |

## 1. 打开链逐段拆解(代码实证,冻结形态 v0.4.1)

| 段 | 做什么 | 出处 | 开销画像 |
|----|--------|------|---------|
| ① 拉起 | 托盘 on_manage → `subprocess.Popen([deskpilot.exe, --whitelist-manager, base_url])`——**新起一个 onefile 进程**:111MB 自解压到 %TEMP%\_MEIxxx + 杀软实时扫描解压物 | main.py:210-226 | **每次开窗都付全价**;慢盘/杀软机可达数十秒(冻结形态独有,源码形态无) |
| ② 单例 | `focus_existing_or_exit("DeskPilot 白名单管理")` 找已有窗 | whitelist_window.py:792 | 毫秒级 |
| ③ 拉数据 | GET /whitelist(本机 HTTP,5s 超时) | whitelist_window.py:746-765 | 取决于 daemon 侧(见④) |
| ④ daemon 侧 | 对静态+会话白名单**每个进程**跑 `app_display_name`+`app_description`(注册表/版本信息/MUI/AppX 清单/开始菜单 .lnk/Get-StartApps powershell 子进程) | httpd.py:255-268 | daemon 启动时 `warm_caches(parallel=True)` 已暖(main.py:484-486);**暖失败/被打断/缓存冷则每进程秒级、串行累加** |
| ⑤ 建窗 | Tk 装配渲染 | whitelist_window.py:779 | 毫秒级 |

**嫌疑排序(机制层,待异机取证坐实)**:A=①onefile 自解压+杀软扫描(冻结形态每开必付);B=④暖缓存失效后冷解析串行累加;C=④中 powershell 子进程被企业策略/性能拖至超时;D=⑤可忽略。

## 2. 现有日志缺口(如实清点)

| 面 | 现状 |
|----|------|
| daemon stderr | 只有启动行,无开窗计时 |
| audit JSONL | 无「管理窗打开」事件、无 /whitelist 耗时、无分段计时 |
| 管理窗子进程 | **零日志**(stderr 随 Popen 丢弃) |

**结论:目前没有任何日志能监控这个过程——慢在哪一段纯属不可观测。**

## 3. 观测面补齐方案(先诊断后方案,SDD)

| # | 埋点 | 落点 |
|---|------|------|
| O1 | 托盘 on_manage 打点(拉起时刻) | daemon audit 事件「管理窗拉起」 |
| O2 | 管理窗进程分段计时:进程启动→聚焦检查→拉取往返(耗时+条目数)→建窗→首帧,各段毫秒 | 子进程 stderr→文件(管理窗日志落受管目录)+完成时回写 daemon audit「管理窗打开耗时」含分段 |
| O3 | daemon /whitelist 处理耗时+逐进程解析超阈值(如 >200ms)留痕 | daemon audit 事件「白名单数据装配」 |
| O4 | 暖缓存状态留痕(warm_caches 完成/失败/耗时) | daemon audit 事件「名称缓存暖机」 |

## 4. 整改方向候选(待观测数据回拍后裁定)

| # | 方向 | 说明 |
|---|------|------|
| ①(倾向) | **管理窗收编为属主进程内 DialogService 线程窗**(ISS-0008 P6 冻结窗同形态)——不再起 onefile 子进程,A 类开销整体消失 | 结构性消除;窗口线程化已有先例 |
| ② | onefile→onedir 或管理窗常驻(首开慢、后续秒开) | 打包形态变更,牵动发布面 |
| ③ | /whitelist 全缓存化+冷路径异步 | 只解决 B/C 段 |

## 5. 约束

- 先诊断后方案:O1~O4 落地后异机复测一轮,按分段耗时数据定整改;
- fail-closed:计时埋点不影响既有行为,观测失败不阻断开窗;
- 本单只到「观测+诊断+整改方向」;实施走 SDD P1→P3。

## 6. 变更记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-09-18 | 建单。sdfang 异机报障(>1 分钟 vs 本机秒开);打开链五段拆解(§1)+日志缺口清点(§2,结论:当前零观测)+观测面四点方案(§3)+整改方向候选(§4,①倾向=收编 DialogService 线程窗) |
