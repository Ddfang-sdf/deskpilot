# ISS-0066:【AI友好】get_clickable_map 输出 id 与 click_element 入参 som_id 命名不一致

| 项 | 内容 |
|----|------|
| 问题单号 | ISS-0066 |
| 标题 | get_clickable_map 返回元素字段叫 id,click_element 的入参叫 som_id——同一棵 SoM 树上的编号两个名字,AI 要自行对应;跨会话/跨文档时易误配 |
| 严重级 | 低(命名一致性;AI 一次性认知成本) |
| 状态 | **已关闭**(2026-09-22 验收通过:som_id双写;sdfang 缺席授权自决,证据见 手工测试计划-20260922-批次②积压验收 X4) |
| 提出 | 2026-09-08 手工测试待查⑥;2026-09-10 转正建单 |

## 现象与证据

get_clickable_map entries[*].id ↔ click_element(som_id=)——同物两名。

## 方向(评审二选一)

| # | 方向 |
|---|------|
| ① | 输出字段改名 som_id(与入参一致;破坏已读该字段的存量脚本) |
| ②(倾向) | 双写过渡:输出同时给 id 与 som_id(同值),描述注明 id 废弃日程;下个大版本删 id |

约束:工具描述同步;用例=输出双字段同值(直出)+click_element 正常消费。

## 变更记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-09-10 | 转正建单(手工测试待查⑥) |
| v0.2 | 2026-09-17 | **P3 完成**。方向②采纳(双写过渡,自主推进授权):core.py get_clickable_map UIA 条目增 `som_id` 与 `id` 同值(id 标废弃日程,下个大版本删);**detect 条目不携 som_id**(其编号不可寻址、只取 rect——不误导消费);描述注明双写与废弃(187 字)。测试 cm01(双写同值)/cm02(描述废弃日程)。**既有钉适配(设计授权契约变更,ISS-0091 先例)**:DET-04「detect=False 零变化」键集钉 五键→六键(+som_id;缺省零调用/零校验断言原样不动);SOM-02 七键钉→按 source 分型(UIA 八键含 som_id 且 som_id==id;detect 七键不含);REQ-003 详设 §5.2 键集表翻页。全量回归 802 passed/25 skipped/0 failed |
| v0.3 | 2026-09-22 | **验收通过关单**(sdfang 离场留言授权自决,记录在案)。证据:手工测试计划-20260922-批次②积压验收 X4,som_id 双写 |
