# 手工测试设计 —— 2026-09-22 四单整改升级验收(AI 执行版)

| 项 | 内容 |
|----|------|
| 对象 | 含 ISS-0093/0100/0101/0102 四单整改的新打包构建(dist/deskpilot.exe 重打包) |
| 执行 | AI 对升级后的**真实 daemon** 操作级验证——真实 HTTP/真实窗口/真实进程,不走 pytest;sdfang 在场目视弹窗面并做身份类动作(点解冻) |
| 设计纪律 | 五要素逐条;断言落在系统外表面直读(HTTP 响应体/盘上文件/窗口矩形/截图像素);逐项回填实测记录 |
| 安全边界 | 冻结用例放最后(W8),解冻由人类点「立即解冻」或 Ctrl+Shift+F11;升级换包=停旧 daemon 父子对→起新构建,属本地升级先例(09-18 v0.4.1 同款) |

## 升级步骤(前置)

1. 停旧 daemon(onefile 父子对:杀子进程,父引导器随退);
2. 起新构建 `dist/deskpilot.exe --daemon`(后台);
3. W1 冒烟确认后进入用例。

## 用例

| # | 场景(对应单) | 前提 | 步骤 | 预期 | 断言(出处) |
|---|------|------|------|------|-----------|
| W1 | 升级冒烟 | 新 daemon 已起 | GET /version + /health + owner.lock/heartbeat | version=0.4.2,health ok,心跳新鲜(pid=新进程) | 响应体直读;盘上文件 ts 直读 |
| W2 | ISS-0093 HTTP 通道消亡 | daemon 在跑 | curl -X POST /estop/reset | 404 NOT_FOUND | 响应体 error_code 直出 |
| W3 | ISS-0093 CLI 消亡 | daemon 在跑 | dist/deskpilot.exe --reset | 无 --reset 分派(不触达复位;按未知参数/主入口语义处理,无「急停已复位」输出) | CLI 输出直读;estop-state.json 内容不变 |
| W4 | ISS-0102 path 落盘+护栏 | daemon 在跑 | ①screenshot region path="assets/_w4.png";②path="C:/Windows/Temp/_w4.png";③path="../_w4.png" | ①落仓库 assets;②③ INVALID_PARAMS 拒,零落盘 | 盘上文件直读;响应体 error_code 直出 |
| W5 | ISS-0101 窗口摆放 | 记事本已 attach | set_window_rect [200,150,900,750] → find_window 读回 | 返回 rect 与读回矩形同口径一致;窗口实摆 | 响应体 data.rect 直出;find_window rect 直读;截图像素 |
| W6 | ISS-0100 读回修真(核心) | 记事本 attach;**中文 IME 激活态** | type_text 纯 ASCII 魔法串 | 落地逐字正确(不再被 IME 吞改);note=「读回校验一致」(TextPattern 实盘);零乱码 | 响应体 note 直出;记事本内容截图/读回直读 |
| W7 | ISS-0100 不一致报码 | 记事本 attach | (构造读回不匹配场景——若实盘难构造,以单测 TC-100-02 绿为准,标注) | TYPE_MISMATCH | 响应体 error_code 直出 |
| W8 | ISS-0093 全链(最后,真冻结) | sdfang 在场 | ①甩角(0,0) 保持冻结 → ②冻结期 curl/写操作拒 → ③**直写 state 文件 frozen:false** → ④仍冻结(E7 关闭) → ⑤人类点「立即解冻」 → ⑥解冻恢复,全程零 estop-reset-*.req | ②EMERGENCY_STOP;④is_frozen 仍 true;⑥健康恢复;零 req 文件 | estop-state.json/响应体直读;LOCALAPPDATA 目录 glob 直读;审计 JSONL「急停复位/冻结提示弹窗」直读 |
| W9 | 回归 | — | pytest 全量默认层 | ≥900 passed 基线,0 failed(四单累计) | pytest 输出直读 |

## 顺序与熔断

- W1→W2→W3→W4→W5→W6→W7→W8(冻结放最后)→W9;
- 任一失败 → 停手记录现场,转回问题单,不连续冲;
- W8 ③写 state 文件后须等 ≥1 个对账周期(~1s)再判 frozen;W8 全程 AI 不碰解冻按钮。

## 实测记录(执行时逐条回填)

| # | 结果 | 证据 |
|---|------|------|
| W1 | **通过** | /version=0.4.2;health ok+pmv2;heartbeat 新鲜(pid=33672 新进程) |
| W2 | **通过** | POST /estop/reset → 404 + error_code=NOT_FOUND(响应体直出) |
| W3 | **通过** | --reset 零复位动作(无「急停已复位」输出);estop-state.json 内容不变(ts=20:32 启动清理) |
| W4 | **通过** | ①落 dist/_w4_shot.png(返回绝对路径直出);②C:/Windows/Temp 拒 INVALID_PARAMS(消息含允许根);③../ 穿越拒;界外零落盘。观测:dist daemon 允许根=dist∪dist/audit(policy 所在目录语义,正确) |
| W5 | **通过** | set_window_rect 返回 rect=[200,150,900,750] 与 find_window 读回逐值一致;before/after 取证图在案;窗口从副屏实摆主屏 |
| W6 | **红=捞到新缺陷 ISS-0104** | type_text 纯 ASCII→读回 fail-closed 连续拦下(dist:READBACK_UNAVAILABLE;源码复现:TYPE_MISMATCH);四层诊断:粘贴落空根因=窗口前台但焦点在标签条(SetFocus 对照组粘贴成功);读回三级通道真机全健康。**已立新单 ISS-0104,改法待裁定** |
| W7 | **通过** | TYPE_MISMATCH 实盘达成(源码形态复现:「粘贴读回校验不一致且重试耗尽」)——fail-closed 零谎报,正是 ISS-0100 C 的设计目标 |
| W8 | **通过** | ①甩角真冻结(审计「急停触发-鼠标甩角」20:53:04);②冻结期 key 拒 EMERGENCY_STOP;③**直写 state frozen:false 模拟 AI 旁路→3s 后写仍拒+state 被修复回 frozen:true**(E7 焊死,本地权威);④LOCALAPPDATA\DeskPilot 零 estop-reset-*.req(只剩 heartbeat/state/owner.lock 三文件);⑤人类点「立即解冻」→**新进程内直调通道打包形态实证**(审计「急停复位-冻结提示弹窗」20:54:37);⑥health ok+move 恢复 |
| W9 | **通过** | pytest 全量默认层:**900 passed / 28 skipped / 0 failed**(215.7s,四单累计后最终基线) |

**结论**:W1~W5/W7/W8/W9 通过;W6 红=捞到新缺陷 **ISS-0104**(type_text 粘贴前未聚焦编辑控件,sdfang 已裁定改法 A=粘贴前 SetFocus,待开发)。四单整改在打包形态全部实盘走通:三通道消亡(W2/W3)、护栏(W4)、窗口摆放(W5)、fail-closed 读回(W7)、冻结全链+旁路焊死(W8)。附带观测:daemon 父子对 20:45 曾自动重启一次(4424 对退出,23348/26404 对 20:45:48 起,复出机制属 ISS-0084 面,健康),留 ISS-0094 诊断参考。
