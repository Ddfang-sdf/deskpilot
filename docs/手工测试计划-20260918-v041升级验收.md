# 手工测试设计 —— 2026-09-18 v0.4.1 本地升级验收(AI 执行版)

| 项 | 内容 |
|----|------|
| 对象 | v0.4.1 本地升级构建(dist/deskpilot.exe,覆盖 2026-09-17 问题单清扫批次 12+ 单) |
| 执行 | AI(Claude)对升级后的**真实 daemon** 操作级验证——走真实 HTTP/文件邮箱/真实窗口,不走 pytest;sdfang 在场目视弹窗面 |
| 设计纪律 | 五要素逐条(场景/前提/步骤/预期/断言);断言全部落在系统外表面直读(HTTP 响应体/盘上文件/窗口矩形/截图像素);先设计后执行;逐项记录实测结果 |
| 安全边界 | 锁屏/UAC 期间 AI 绝对禁止一切操作(含截屏)——M6 需人类配合解锁;身份类动作(UAC 点按)永远人类执行;冻结测试放在最后(会真冻结) |

## 用例

| # | 场景 | 前提 | 步骤 | 预期 | 断言(出处) |
|---|------|------|------|------|-----------|
| M1 | 升级冒烟 | 新 daemon 已起 | GET /version + /health + 托盘/属主面文件 | 版本=0.4.1,health ok,属主面正常 | 响应体直读;owner.lock/heartbeat 盘上直读 |
| M2 | ISS-0089 降采样 | daemon 在跑 | POST /call screenshot fullscreen | 内联图长边≤2000 等比;path 落盘全分辨率;scale_x=scale_y<1 | 响应 data 直读+PIL 解码内联/落盘图尺寸直读 |
| M3 | ISS-0088 深度链 | 真 mspaint | get_ui_tree 找深度≥9 ButtonControl → click_element 类型路径 | ok 且命中 ButtonControl(非外壳) | 响应体 element.control_type 直出 |
| M4 | ISS-0073 入白证据面 | mspaint 未入白(生产 policy 核对) | attach(mspaint) → 入白弹窗弹出 → screenshot fullscreen 截弹窗 → 等超时默认拒绝 | 弹窗含实拍图+显示名来源+自报标注+exe 路径;超时拒 | 弹窗截图 OCR/像素直读;attach 响应 APPROVAL_TIMEOUT 直出 |
| M5 | ISS-0071 撤回窗落位 | 白名单有条目 | request_remove_from_whitelist → 确认窗弹出 → 读窗矩形 | 窗矩形 ⊆ 主屏右下区(x≈屏宽-436,y≈工作区底-130) | find_window/窗口矩形直读;超时默认保留 |
| M6 | ISS-0087 安全桌面 | **需人类配合**:用户锁屏 Win+L 再解锁 | 锁屏期间 AI 调 get_cursor(只读也算) | 锁屏期返回 SECURE_DESKTOP;解锁后自动恢复 | 响应体 error_code 直出;**锁屏期间 AI 不发任何调用以外的动作**;解锁由人类完成 |
| M7 | ISS-0092 解冻链(最后做) | daemon 在跑 | 写 estop-state.json 触发?**不,真触发**:甩角到左上角保持 1.5s → 冻结 → 写 estop-reset-{seq}.req → 等消费 | 冻结期间写操作拒 EMERGENCY_STOP;req 被消费后 frozen=false 自动恢复;审计含新四类事件之一可观测 | estop-state.json 盘上直读;/call 响应码直出;审计 JSONL 直读 |
| M8 | 回归冒烟 | — | get_cursor + move(小位移回原) + key(无害键入记事本?否——改 get_ui_tree 只读) | 全部 ok | 响应体直出 |

## 顺序与熔断

- M1→M2→M3→M4→M5→M8→(M6 需人类在场配合段)→M7(冻结链放最后,测完即恢复验证);
- 任一用例失败 → 停手记录现场(响应体/盘上文件/审计),不连续冲;
- M6 锁屏期间 AI 零动作(含截屏)——解锁后补验;若用户不便锁屏,M6 标注「留人类择机验」。

## 实测记录(执行时逐条回填)

| # | 结果 | 证据 |
|---|------|------|
| M1 | **通过**(附异常记录) | /version=0.4.1、/health ok、PMv2;owner.lock/心跳/遗嘱面正常。**异常记录**:首次换包启动后 owner.lock/heartbeat 内容滞留旧 pid(25392/0.3.7,死亡进程的),重启后正常——一次性现象未坐实(疑似与被杀前驱的 OS 放锁窗口期竞态),已观察记录 |
| M2 | **通过** | stdio MCP 实测:fullscreen 3840×1081 → 内联 2000×563 等比;落盘 3840×1081 全分辨率;scale_x=scale_y=0.5208=2000/3840 精确 |
| M3 | **通过** | 真画图 229 元素、23 个深度≥9 具名钮;「直线」depth 9 经 control_type+name 路径点击 ok,响应 element.control_type=ButtonControl(命中内层非外壳) |
| M4 | **通过** | launch_app(calc.exe) 入白弹窗:OCR 实证弹窗含「显示名来源/自报/未经系统核验/程序路径/.exe/Calculator」六要素+三态按钮;desc 文件实证「未找到目标软件的窗口,未截图;本次无实拍,请谨慎裁决」(calc.exe 是 UWP stub,反查不到=诚实无实拍分支真实走通);90s 超时 APPROVAL_TIMEOUT 默认拒绝 |
| M5 | **通过** | 撤回确认窗矩形 (1484,886,1904,1016) 与统一落位公式逐像素一致(主屏右下);超时默认保留 removed=False 零变更 |
| M6 | **未执行** | 需人类锁屏配合(锁屏期 AI 零动作铁律)——留用户择机:Win+L 后 AI 任何调用应得 SECURE_DESKTOP,解锁自动恢复 |
| M7 | **中断=捞到两个真问题** | ①冻结形态构建 req 消费面失效(两次写 req 不消费不删,源码形态 0.2s 正常)——随 ISS-0093 通道移除而废止;②**升级为安全漏洞 ISS-0093**:AI 直接写 req 文件自行解冻实证,sdfang 裁定「不暴露任何接口和文件,指令直达」 |
| M8 | **通过** | get_cursor/move 往返复原(458,92) |

**结论**:v0.4.1 升级批次功能面全部通过;M7 捞出的安全漏洞已升单 ISS-0093(严重级),其通道移除后 M7 的消费缺陷自然消亡。daemon 当前为冻结形态 0.4.1 健康运行(心跳/属主锁/版本三证齐)。
