# 手工测试设计 —— 2026-09-22 批次②积压验收(AI 执行版)

| 项 | 内容 |
|----|------|
| 对象 | 批次② P3 绿积压单的可直接执行子集(11 单),对当前 daemon(21:58 构建,含今日全部整改)实盘验证 |
| 执行 | AI 执行取证,sdfang 末批裁定关单(同批次①模式) |
| 纪律 | 五要素逐条;断言在外表面直读;需人类配合/特定环境的单列不混入 |

## 用例

| # | 对应单 | 场景 | 预期(断言出处) |
|---|--------|------|----------------|
| X1 | ISS-0037 | screenshot 返回 vision_note 含 ocr(source=<path>) 指引;ocr=true 附 ocr_items/ocr_error;默认无 ocr_items 键 | 响应体直出 |
| X2 | ISS-0039 | 全屏 OCR 不再 TOOL_TIMEOUT(预算覆盖首推) | 响应体直出 |
| X3 | ISS-0065 | find_window(hwnd=真句柄) 直查命中不被拦 | 响应体直出 |
| X4 | ISS-0066 | get_clickable_map UIA 条目 id==som_id 双写;detect 条目不携 som_id | 响应体直出 |
| X5 | ISS-0069 | ocr 描述无「屏幕区域」假形态(注册表直读);ocr(source=path) 命中 | 注册表+响应体 |
| X6 | ISS-0070 | type_element 空选择器 → INVALID_PARAMS 含「至少提供一个」 | 响应体直出 |
| X7 | ISS-0072 | 撤回墓碑→attach 零弹窗 REVOKED_BY_HUMAN(**需人类在撤回确认窗点确认**) | 响应体直出 |
| X8 | ISS-0075 | list_desktop_icons graphic_rect=虚拟桌面坐标 | 响应体直出 |
| X9 | ISS-0081 | 取图 A→变布局取图 B→A 的过期编号点:ELEMENT_NOT_FOUND 零点击 | 响应体+光标不动 |
| X10 | ISS-0052 | 重启 daemon 后即刻 /call 得结构化 JSON 错误体(非裸 500) | 响应体直出 |
| X11 | ISS-0091 | 退化矩形拒点(单测 cg03/04 已钉)+实盘回归:正常按钮可点、estop 未冻结 | 响应体+state 直读 |

## 实测记录(执行时逐条回填)

| # | 结果 | 证据 |
|---|------|------|
| X1 | **通过**(ISS-0037) | vision_note 含 ocr(source=<本图路径>)✓;ocr=true 附 ocr_items(97 项)且 path 未受损✓;默认无 ocr_items 键✓ |
| X2 | **通过**(ISS-0039) | 全屏 OCR 一次通过,err="",97 items,无 TOOL_TIMEOUT |
| X3 | **通过**(ISS-0065) | find_window(hwnd=1248248) 直查命中,无 INVALID_PARAMS |
| X4 | **通过**(ISS-0066) | get_clickable_map count=66,UIA entries 逐条 id==som_id 双写(1~5 号抽样+全量校验) |
| X5 | **通过**(ISS-0069) | ocr 描述无「屏幕区域」假形态(注册表直读);ocr(source=path) 命中 43 items |
| X6 | **通过**(ISS-0070) | 空选择器→INVALID_PARAMS「参数 name/automation_id 至少提供一个」逐字直出 |
| X7 | **待人类配合**(ISS-0072) | 需人类在撤回确认窗点「确认移出」才能写墓碑;留 sdfang 择机:request_remove_from_whitelist→确认→attach 应零弹窗 REVOKED_BY_HUMAN |
| X8 | **通过**(ISS-0075) | list_desktop_icons items 含 graphic_rect(回收站 [0,5,76,57] 等),虚拟桌面坐标口径 |
| X9 | **通过**(ISS-0081) | 图A=66→set_window_rect 变布局→图B=51→点 A 的过期 som_id=60:ELEMENT_NOT_FOUND「请重新调用 get_clickable_map 取图」,光标 (723,974) 纹丝不动零点击;窗口已复原 |
| X10 | **通过**(ISS-0052) | 重启 daemon 后即刻 /call(bogus token):结构化 JSON `{"ok":false,"error_code":"NO_BINDING",...}`——非裸 500 |
| X11 | **通过**(ISS-0091) | 实盘回归:click_element(name=「不保存」)经守卫链正常命中执行(XAML 保存提示消解,窗口正常关闭);estop-state frozen:false 未诱发冻结;退化矩形拒点由 cg03/04 钉守(904 绿套件内) |

**结论**:可直接执行子集 10/10 通过(X7 留人类配合)。另:W8 已顺带覆盖 ISS-0092 残余面(冻结全链+本地权威对账+零 req)、V3 覆盖 ISS-0071 落位语义、V4 覆盖 ISS-0073 英文面、W1 覆盖 ISS-0084 心跳/属主面——建议 sdfang 按各单残余面裁定是否直接关闭;环境依赖组(ISS-0087 锁屏/ISS-0088 mspaint/ISS-0089 双屏降采样/ISS-0007/0012/0015/0031~0035/0044/0045④)另约。

## 追加执行(2026-09-22 晚,本机环境可满足的三单)

| # | 对应单 | 结果 | 证据 |
|---|--------|------|------|
| X12 | ISS-0089 双屏降采样 | **通过** | stdio MCP 实测:fullscreen 3840×1081 → 落盘全分辨率+scale_x=scale_y=0.5208333=2000/3840 精确;内联图 2000×563(长边≤2000、宽高比守恒,PIL 直读) |
| X13 | ISS-0088 深度链(真 mspaint) | **通过** | 真画图 attach→click_element(control_type=ButtonControl,name=「矩形」)→ok,响应 element.control_type=ButtonControl(命中内层钮非外壳) |
| X14 | ISS-0007 双屏副屏 | **通过** | 副屏记事本(hwnd 200642,rect[1950,77,3840,1011])attach→click(2900,500) ok→type_text「iss7副屏」note=读回校验一致——副屏虚拟坐标点击/输入落点正确 |
| X15 | ISS-0035 双贴回归 | **通过** | type_text「双贴回归中文串ABC」note=读回校验一致;UIA 直读文档:该串出现恰 1 次、「iss7副屏」恰 1 次——零双贴 |
| X16 | ISS-0033 审批 TTL | **通过** | alt+f4(L3)挂起整 90s(curl max-time 110 不断链)→结构化 APPROVAL_TIMEOUT 返回(消息含窗口标题与句柄),超时默认拒记事本未关 |

**环境依赖组收尾状态**:仅剩 ISS-0087(锁屏,需人类 Win+L 配合)、ISS-0012(三态点击)、ISS-0015(MCP 客户端观察项)、ISS-0031/0032/0034(时限/迁移/CI 场景)、ISS-0044(清桌面)、ISS-0045④(原则裁定)、ISS-0072(撤回确认点击)——全部需人类在场或特定环境,另约。**(ISS-0033/0035 已于 X15/X16 收编通过)**
