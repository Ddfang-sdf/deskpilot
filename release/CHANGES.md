## v0.4.1(2026-09-18,本地升级构建)

- **急停状态机灾难恢复**(ISS-0092):甩角/热键线程异常守卫(单轮异常不杀线程)、共享状态写回有限重试+孤儿清理、解冻请求先复位后删、双向对账自愈(禁止反向绕过人类解冻)
- **click 像素兜底退化守卫**(ISS-0091):退化矩形拒点 ELEMENT_RECT_DEGENERATE+校验链接入,不再误点屏幕原点诱发甩角冻结
- **screenshot 内联图源头等比降采样**(ISS-0089):长边>2000px 自动等比缩(客户端拒收根治),落盘全分辨率不动,scale 契约=virtual_rect 原点+内联像素/scale
- **安全桌面全禁**(ISS-0087):锁屏/UAC 期间显式检测并全禁 AI 操作含截屏(SECURE_DESKTOP),回桌面自动恢复
- **UIA 深度截断单源化**(ISS-0088):取树/定位/SoM 同口径深度 10+嵌套同名最内层胜出(画图形状钮类型路径可点)
- **入白审批证据链重写**(ISS-0073):五点采样可辨认度、置前成败如实区分、隐藏窗不取证、显示名证据强度序(FileDescription 优先)+自报标注+exe 全路径底牌
- **白名单浮窗落位统一**(ISS-0071):撤回确认窗/入白回执跟随被裁决对象所在屏右下角
- **描述/schema 一致性批**(ISS-0065/0066/0069/0070):find_window 支持 hwnd、SoM 条目 som_id 双写、ocr/type_element 描述纠偏
- **cleancode**(ISS-0056/0057/0061):FAILSAFE 收敛单点、任务栏避让单源+工作区真查、心跳锁死代码清除
- 文档对齐批(ISS-0067/0068/0074~0079):错误码登记册补登 7 码+钉守口径、REQ-001/002 测试设计回流
- 测试:848 用例全绿(单元+集成;含真机 mspaint/真 ctypes 建窗/真鼠标通道)

## v0.3.7

- **GUI 图形检测与 SoM 扩面**（REQ-003，D-12 终裁 CV 单线出货）：`get_clickable_map` 增 `detect` 开关——CV 管线（`cv-contour`，零权重零下载零新依赖）把 UIA 看不见的画布图形/自绘控件框出并入单一编号空间；`source=uia/detect` 双通道，UIA 优先去重（判据集取枚举全量、减结构容器含宿主型 CustomControl，D-17/D-20）；检测编号不可经 `som_id` 点击（误用报指引级错误，零点击）；`detect=false` 默认零变化；`DETECTOR_UNAVAILABLE` 新码登记
- **daemon 属主与生命周期族**（ISS-0084）：`owner.lock` 属主文件锁 + 心跳/遗嘱/死亡告警（托盘气泡）+ 开机自启幂等注册——daemon 静默死亡不再无迹；daemon 不在时 stdio 实例经属主锁升属主（9420/热键/托盘随 MCP 存活），daemon 复出自动让位回迁；热键争抢改节流（只记首败+恢复，刷屏终止）；属主面文件与急停邮箱锚定 `%LOCALAPPDATA%\DeskPilot`（双邮箱分裂收口）
- **screenshot 按屏取图档与覆盖率**（ISS-0083）：`scope=screen`（screen=屏号，即 fullscreen 的 monitors 序号，双屏不再让 AI 自算偏移；越界屏号 fail-closed）；返回值增 `coverage`（本图占虚拟桌面比例，AI 自见截取比）；region 定位注明「精读/局部核对用」
- **撤回即否决**（ISS-0072）：白名单墓碑经闸二硬拒 `REVOKED_BY_HUMAN`（零弹窗/零取图/零抢焦点）；缺省装配注入 policy.revoked
- **白名单补 ctrl+a**（全选，与 ctrl+c/v/x 同族，sdfang 当次下令当次落盘）
- 测试：758 用例全绿（默认零副作用）+ 集成用例真机终验收（TC-INT-01/02/03 检测主链/误用防护、TC-GOV-03b 预算实测）；真机验收作业（画图房子与树经检测通道框出编号）见 docs/需求/REQ-003-侦察/ 与手工测试记录

## v0.3.6

- **无文字图形定位**（ISS-0044）：`get_ui_tree` 增 control_type 过滤；`click_element` 支持 类型+序号 寻址（歧义不再死锁）；`click_text` 增 offset/distance 相对点击（锚点文字上/下/左/右邻位）——图标/复选框等无文字图形不再靠猜坐标
- **DPI 感知真正生效**（ISS-0050/0051）：修复 ctypes 封送溢出（`c_void_p(-4).value` 巨数按 c_int 封送必抛被静默吞）——Per-Monitor V2 自 ISS-0007 起从未真正声明成功，实盘一直跑 V1；回报改查询制，`/health` 暴露 `dpi_mode`，混合缩放双屏从此可诊断
- **批量授权生效面修复**（ISS-0053【设计引入】，收敛 ISS-0043/ISS-0019 §6）：会话许可键改 hwnd+tool（重新 attach 不再失忆）；危险键按**同键粒度**批量（esc 免批不捎带 delete）；终端类维持逐操作（自由文本载荷结构上不批量）；审批弹窗作用域提示同步新语义
- **大文本级联修复**（ISS-0045）：读回轮询窗口按文本规模缩放（300ms×⌈len/8k⌉，3~20 拍），65k 字实测 0.6s 一次贴入；参数校验前置写锁之外，超限输入秒拒 INVALID_PARAMS 不再排队耗成 TOOL_TIMEOUT
- **daemon 单例与冻结弹窗去重**（ISS-0046）：`--daemon` 预检已有属主即审计+退出码 4（拒绝双起僵尸）；冻结弹窗单例互斥收口建窗点——一次冻结只弹一框
- **drag 跨屏/移动窗口**（ISS-0047）：拖拽终点校验改虚拟桌面逐屏判定（起点防误射不变、并集包围盒死角仍拒、枚举失败 fail-closed）——移动窗口到副屏不再被误拒
- **启动健壮性**（ISS-0048【修改引入】/ISS-0049）：启动抬键清扫逐键容错 FAILSAFE（光标压角不再启动即崩）；甩角电平触发改边沿触发（首采基线/复位再基线）——压角残留重启不再启动即冻结，甩角语义不变
- **桌面图标装配按名归并**（ISS-0054【设计引入】）：自定义排列/桌面编辑后 PIDL 枚举序与 UIA 可视序分叉，旧索引对齐致图标感知全灭——改按名归并（虚拟项恒 null、重名歧义/无匹配来源 fail-closed 附指引）
- 测试：635 用例全绿（默认零副作用）；icons09 集成断言改环境不变量（原活桌面阈值在干净 CI runner 必败，测试卫生修复）；本版全部修复含真机活实证链（docs/手工测试记录-20260908.md 批次 H）

## v0.3.5

- **activate 最大化保持**（ISS-0041）：激活窗口不再把最大化打回原始尺寸——ShowWindow 命令按窗口状态选择（最小化恢复/最大化保持/普通不动）；工具描述增几何变化后先重新感知指引
- **screenshot 感知自愈**（ISS-0037）：响应附 `vision_note` 降级指引（图像不可见时改调 `ocr(source=路径)`，零摸索）；`ocr:true` 一次调用返回图像+文字清单（OCR 失败显式附 `ocr_error`，图像不受损）；严格 bool 参数校验
- **OCR 超时指引与预算覆盖**（ISS-0039）：ocr/screenshot 专属时限预算 12s（实测全屏 CPU 推理首推 5.2s 超旧 5s 预算，负载下必然超时）；ocr 描述明示「局部实拍优先+超时按指引重试」
- **工具清单构建修复**（ISS-0040【修改引入】）：inputSchema 类型映射补齐 bool，修复 list_tools 全量 KeyError 致 MCP 客户端「tools fetch failed」
- **README 加白管理能力**：入白审批与白名单管理实拍截图、双文件政策（policy.yml 基础 / policy.local.yml 用户数据，升级不丢、移出即墓碑）
- **需求演进体系**：docs/需求/ 建立 REQ-001 鼠标能力补全 / REQ-002 桌面图标感知（原始输入随单归档）
- 测试：536 用例全绿（默认零副作用）

## v0.3.4

- **按文字点击 click_text**（ISS-0021）：OCR 定位文字→自动换算虚拟桌面坐标→与裸坐标点击同安全链（防误射/遮挡校验/证据图）；未命中/多命中 fail-closed；多命中必须显式 index；screenshot 返回坐标系元数据（virtual_rect/scale）
- **按键可发现**（ISS-0022）：未收录键错误返回完整可用键清单并明示「本次未发送任何按键」；浏览器标签/窗口组合键（ctrl+t/n/l/tab/r）收 L3 本地审批
- **TOOL_TIMEOUT 重试指引**（ISS-0023）：超时响应带 retry_after_ms 与建议次数，AI 可自愈重试
- **审批超时链修复**（ISS-0024/0033）：90 秒审批期内调用不再中途报超时（此前 30 秒即断线，「报错但动作可能已执行」）；客户端超时改由策略推导，消除魔法常量
- **AI 可自愈错误规范**（ISS-0027）：参数类错误回显服务端实收值；OCR 未命中附相似文本建议（仅建议、无载荷、不自动执行）
- **测试卫生**（ISS-0025/0026/0029）：默认 pytest 零副作用（不开真实窗口、不读生产目录），真机集成需 `--run-integration`；用例独立（随机序 CI 防护）；删除死代码登记册
- **甩角阈值 200→1000ms**（ISS-0028）：消除鼠标路过角落的误冻结
- **出厂策略与用户数据双文件分离**（ISS-0030/0032）：永久入白写入 `policy.local.yml`，升级/重装自动保留（`--migrate-policy` 迁移）；撤回记录永续（墓碑）；用户数据文件无权放宽任何安全参数
- **审计数据保留策略**（ISS-0031）：日志保留 90 天；截图 90 天+450MB 双阈值（合计 ≤500MB，容量为硬顶）
- **守护机制修正**（ISS-0034）：服务自身入白写入不再误报外部篡改；策略同步校验接入 CI（漂移即红）
- **文字通道修正**（ISS-0035）：type_text 读回轮询确认，消除滞后重贴导致的文档内容重复；点击/建议共用大小写不敏感归一化
- 测试：515 用例全绿（默认零副作用）

## v0.3.3 及更早

- **常驻 daemon 形态**（ISS-0001）：服务进程常驻，MCP 调用不再每次重启进程，单次调用约 1.2s；绑定跨调用保持
- **急停复位通道**（ISS-0002）：冻结后 `Ctrl+Shift+F11` 或 `deskpilot.exe --reset` 复位；热键注册失败退避重试不静默放弃
- **L3 审批批准即执行**（ISS-0003）：批准后同一调用内同步执行，不再只批不动
- **冻结人类感知**（ISS-0004/0005/0006）：冻结即时弹窗（警示圆角卡片滑入滑出、立即解冻/稍后提醒）、单弹窗不重复开、解冻请求不错序消费
- **性能优化**（ISS-0008）：daemon 线程化、OCR 懒加载、证据图 JPEG、窗口枚举去重、空闲自停；审批挂起期 /health 38~69ms
- **Claude 客户端断链可靠性**（ISS-0009）：进度通知、超时预算、stdout 卫生、版本握手
- **截图受管目录与时空双阈值清理**（ISS-0010）：截图统一落受管目录，按容量/时长双阈值自动清理
- **多显示器（双屏）支持**（ISS-0007）：审批/冻结弹窗跟随目标窗口/鼠标所在屏；截图与 UI 树声明虚拟桌面坐标系（含负坐标）；DPI 感知升级 Per-Monitor V2；急停触发角文档化为主屏左上角
- **发布自动化与安装指导**（ISS-0011）：tag 即发布的 GitHub Actions 流水线、INSTALL.md 安装指导书、install.ps1 一键安装脚本
- **白名单审批入白与全可视化管理**（ISS-0012）：非白名单软件弹窗三态入白（本次会话允许/永久加入/拒绝），永久加入由系统原子写盘；系统托盘图标+白名单管理窗口（搜索/分区/更多/描述）；AI 请求撤回确认窗；策略文件指纹审计；deskpilot.exe 自保护永不可入白
- **弹窗静默修复**（ISS-0013）：弹窗线程默认工厂接线（曾致生产弹窗全哑）；测试设计五条硬规则（装配守门）
- **工具描述意图化**（ISS-0015）：24 个工具意图化描述（用途/场景/参数返回/组合链）+ server 级领域划界指令（Windows 桌面 vs 浏览器路由）
- **窗口定位与读取可靠性**（ISS-0016）：UIA 线程级 COM 惰性初始化，错误分类（UIA 通道异常 vs 窗口消失），搜索无果给进程名指引
- **窗口激活容错与提权边界**（ISS-0017）：激活还原+前台锁退避；写前提权检测（目标以管理员运行显式报错 ELEVATION_REQUIRED）；落点遮挡判定
- **路径与配置一致性**（ISS-0018）：截图返回绝对路径；policy.yml 单源化（构建同步+哈希一致）
- **审批批量授权**（ISS-0019）：「此后同类允许」按钮——同窗口同工具会话内后续同类免批（仅内存令牌；终端与危险键仍逐次审批）
- **审批可读性**（ISS-0020）：主标题直接显示命令文本/坐标；无绑定审批反查实拍；取图失败审计留痕；弹窗图像 GC 空白修复
- **审批截图四步契约**（ISS-0020 续）：找到目标窗口→前置→无遮挡校验→才截图；修入白审批漏传请求致弹框无图；废全屏静默退化，拍不到就明示不给错图
- 测试：434 用例全绿
